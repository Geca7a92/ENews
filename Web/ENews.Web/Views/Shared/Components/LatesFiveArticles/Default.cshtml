@model ENews.Web.ViewModels.Home.IndexViewModel

<div class="card">
    <div class="card-header">
        <h3>Latest news</h3>
    </div>
    <div class="card-body">
        @foreach (var article in Model.LatestTwoArticles)
        {
            @await Html.PartialAsync("_ArticleNavPartial", article)
            <hr />

            @*<div class="single-popular-post">
                <a asp-area="" asp-controller="Articles" asp-action="Index" asp-route-id="@article.Id">
                    <h6> @article.ContetPreview</h6>
                </a>
                <p>@article.CreatedOn.ToString("MMMM dd")</p>
            </div>*@
        }
    </div>
</div>